# AI Impact Analysis - Environment Configuration
# Copy this file to .env and fill in your actual values

# Jira Configuration (Required for Jira reports)
JIRA_URL=https://issues.redhat.com
JIRA_API_TOKEN=your_jira_api_token_here
JIRA_PROJECT_KEY=Konflux UI

# GitHub Configuration (Required for PR reports)
GITHUB_TOKEN=your_github_token_here
GITHUB_REPO_OWNER=your-org-name
GITHUB_REPO_NAME=your-repo-name

# Google Sheets Configuration (Optional - for auto-upload)
# Use absolute path (e.g., /home/username/.config/google/credentials.json)
# Note: Don't use $HOME or other shell variables - docker-compose won't expand them
GOOGLE_CREDENTIALS_FILE=/path/to/service-account-key.json
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here

# Anthropic API Configuration (Optional - for AI-powered analysis via API)
# Get your API key from: https://console.anthropic.com/
# If not set, Claude Code CLI will be used instead
ANTHROPIC_API_KEY=sk-ant-your_api_key_here

# Email Notifications for Anonymous Identifiers
# When CI generates anonymized reports, each team member receives ONLY THEIR OWN anonymous ID
# (e.g., Developer-A3F2) - privacy protected, they cannot see others' IDs
# Members can optionally share their ID with managers to connect their work to reports
#
# Configuration in team YAML files (jira_report_config.yaml or pr_report_config.yaml):
#   email_anonymous_id:
#     enabled: true  # Set to false to disable
#
# SMTP Server Settings (optional - defaults to Gmail):
SMTP_HOST=smtp.gmail.com  # Default: smtp.gmail.com (Gmail)
SMTP_PORT=587  # Default: 587 (TLS)
FROM_EMAIL="ImpactLens <your_email@example.com>"  # Optional: Custom sender name
#
# SMTP Credentials (required):
# For CI: Use GitHub Secrets named MAIL_APP_USER and MAIL_APP_PASSWORD
# For local: Use environment variables below
SMTP_USER=your_email@example.com  # Your email address
SMTP_PASSWORD=your_app_password_here  # Gmail App Password or SMTP password
#
# Common SMTP Settings for Different Providers:
# Gmail:    smtp.gmail.com:587 (use App Password: https://support.google.com/accounts/answer/185833)
# Outlook:  smtp-mail.outlook.com:587
# Yahoo:    smtp.mail.yahoo.com:587
# Office365: smtp.office365.com:587

# Note: Never commit the actual .env file to version control!
