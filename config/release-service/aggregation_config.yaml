aggregation:
  # Name for the aggregated report
  name: "Release Service Team"

  # Output directory for aggregated reports
  output_dir: "reports/release-service-aggregated"

  projects:
    - "release-service"
    - "release-service-catalog"
    - "release-service-utils"
    - "internal-services"
